# Environment Management

Provide a template `.env.example` with placeholders only.

Never place actual credentials into committed documentation.

Example variables:
```text
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
```
